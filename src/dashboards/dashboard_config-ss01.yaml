title: "SS01 (wucaishen) Analytics"

stats_by_date: 
  files: [
    "/Users/niuxin/Documents/aws-example/jobs/output_ss01_wucaishen/stats_by_date.parquet",
  ]

  group_col: "ai_group"
  date_col: "activity_date"

  group1_columns: [
    "total_daily_users",
    "ai_group_users",
    "default_group_users",
    "day0_num_users",
    "day1_num_users",
    "day2_num_users",
    "ai_day0_num_users",
    "ai_day1_num_users",
    "ai_day2_num_users",
  ]

  group2_columns: [
    "default_rtp",
    "ai_rtp",
    "default_num_bets_per_user",
    "ai_num_bets_per_user",
    "default_total_bet_per_user",
    "ai_total_bet_per_user",
    "default_profit_per_user",
    "ai_profit_per_user",
  ]

  group3_columns: [

  ]

stats_by_bet: 
  files: [
    
  ]

  group_col: ""

